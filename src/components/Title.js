import React from "react";
import { Title } from "../styledcomponet/StyledComponent";

const TitleComponent = () => {
  return (
    <Title>we're launching soon</Title>
  );
};

export default TitleComponent;
